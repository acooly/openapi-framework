<?xml version="1.0"?>
<project
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
        xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yiji</groupId>
        <artifactId>openapi-framework</artifactId>
        <version>4.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>acooly-openapi-assemble</artifactId>

    <properties>
        <deploy.skip>true</deploy.skip>
        <acooly.secutiry.version>4.0.0-SNAPSHOT</acooly.secutiry.version>
    </properties>

    <dependencies>

        <!-- 服务集成 -->
        <dependency>
            <groupId>com.yiji</groupId>
            <artifactId>openapi-framework-core</artifactId>
        </dependency>

        <dependency>
            <groupId>com.yiji</groupId>
            <artifactId>openapi-framework-nms</artifactId>
        </dependency>

        <dependency>
            <groupId>com.yiji</groupId>
            <artifactId>openapi-framework-test</artifactId>
        </dependency>

        <dependency>
            <groupId>com.yiji</groupId>
            <artifactId>openapi-framework-manage</artifactId>
        </dependency>

        <!-- boss框架 -->
        <dependency>
            <groupId>com.acooly</groupId>
            <artifactId>acooly-module-security</artifactId>
            <version>${acooly.secutiry.version}</version>
        </dependency>

        <dependency>
            <groupId>com.acooly</groupId>
            <artifactId>acooly-module-security</artifactId>
            <classifier>static</classifier>
            <version>${acooly.secutiry.version}</version>
        </dependency>

        <dependency>
            <groupId>com.acooly</groupId>
            <artifactId>acooly-taglibs</artifactId>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>


</project>
